# Human Test Plan: /demo Page (2026-05-13-demo-page)

Generated from: `docs/implementation-plans/2026-05-13-demo-page/`
HEAD at plan completion: `ae96925`

## Coverage Validation Result: PASS

**Automated Criteria:** 0 | **Covered:** 0 | **Missing:** 0
**Build-Gate Criteria:** 1 | **Verified Present in Code:** 1

### Build Gate
| Criterion | Gate Location | Verifies |
|-----------|---------------|----------|
| AC2.4 (qa shape) | `apps/web/app/(app)/demo/QAShowcasePanel.tsx:5` — `const result: AskResponse = qaData as AskResponse;` | TypeScript compile fails if `example_qa.json` loses a required `AskResponse` field. CI runs Next.js build which invokes `tsc`. |

The test-requirements document intentionally declares zero automated tests. Justification is grounded in verifiable codebase conventions (no precedent for testing Server Components or `fetch`-on-mount components; Imperative Shell scripts verified operationally per FCIS convention in `CLAUDE.md`).

---

## Prerequisites

- Fresh checkout of branch at `HEAD ae96925`.
- Node 20 LTS, Python 3.11, all `make setup` deps installed.
- `make test` passes baseline (regression gate — independent of these ACs).
- For live-mode scenarios only: valid `GOOGLE_API_KEY` in `.env`, R2R config (`r2r_gemini.toml`) present.
- Browser with DevTools (Chrome/Firefox/Edge).

---

## Phase 1: Nav and Routing (AC1)

| Step | Action | Expected |
|------|--------|----------|
| 1.1 | Run `make web`. Open `http://localhost:3000/notebooks`. | Page loads. Left sidebar visible (≥ md breakpoint). |
| 1.2 | In the sidebar, locate the nav list. | A "Demo" item is present with a Sparkles icon. **No** "Evaluations" item is present anywhere in the sidebar. Order top-to-bottom: Notebooks, Ask, Conversations, Documents, Workflows, Demo. |
| 1.3 | Click "Demo" in the sidebar (or navigate directly to `http://localhost:3000/demo`). | URL becomes `/demo`. Page renders with `<h1>` "Demo". Four sections visible by heading text: "Wiki Generation", "Cited Q&A", "Figure Extraction", "Try It Live". No white screen, no 500, no Next.js error overlay. |
| 1.4 | Navigate directly to `http://localhost:3000/evals`. | Page renders the RAGAS evaluations table (the underlying `apps/web/app/(app)/evals/` route file was preserved; only the sidebar entry was swapped). No 404. |
| 1.5 | Open DevTools → Console + Network. Hard-refresh `/demo`. | Console: no red errors, no uncaught promise rejections. Network: no 404s (notably, `/demo/example_figure.png` returns 200). |
| 1.6 | Navigate to `/demo` and back to `/notebooks` repeatedly. | Active-route highlight in sidebar moves correctly. No stale loading state retained on `/demo`. |

---

## Phase 2: Static Showcase Panels (AC2)

**Setup:** Run **only** `make web` (do NOT start `make api` or `make r2r`). This isolates the "static panel must not call the backend" assertion.

| Step | Action | Expected |
|------|--------|----------|
| 2.1 | Open `/demo`. DevTools → Network → filter "Fetch/XHR". Clear the network log, then hard-refresh. | Wiki Generation panel renders at least one section title. Network shows **zero** requests originating to `localhost:8000` or any backend host triggered by this panel. |
| 2.2 | In the same view, locate the "Cited Q&A" panel. | Panel shows the answer markdown from `example_qa.json`. At least one `[1]`-style citation badge is present inline in the prose and is clickable. Network still shows zero backend calls attributable to this panel. |
| 2.3 | Locate the "Figure Extraction" panel. Inspect the `<img>` element and Network entry for `/demo/example_figure.png`. | An `<img>` is rendered. Caption text is non-empty and references `make demo-setup`. The `/demo/example_figure.png` request returns 200. (Pre-Phase-5: 1×1 placeholder is acceptable; post-Phase-5: a real figure ≥ 1024 bytes.) |
| 2.4a | **Build leg (AC2.4 primary).** From `apps/web/`: edit `apps/web/app/(app)/demo/data/example_qa.json` — delete the `"figures": []` line (or any required `AskResponse` field, e.g., `"answer"`). Run `npx tsc --noEmit`. | `tsc` exits non-zero with a "Property 'figures' is missing in type" (or equivalent missing-property) error pointing at `QAShowcasePanel.tsx`. |
| 2.4b | Restore the deleted field. Re-run `npx tsc --noEmit`. | Exits 0. |
| 2.4c | **Operational leg (wiki shape).** With real Phase-5 `wiki_structure.json` in place, hard-refresh `/demo`. | WikiPreviewPanel renders without runtime errors; the section list reflects the real notebook structure. Console shows no exceptions from JSON-shape mismatches. |

---

## Phase 3: DemoAskBox Degradation (AC3)

This phase covers the three states of `DemoAskBox`: `loading`, `live`, `offline`. Use `.env.local` in `apps/web/`; remember Next.js bakes `NEXT_PUBLIC_*` at build time — restart `make web` after every `.env.local` edit.

### 3a — Live state (AC3.1)

Prerequisites: Phase 5 complete (real "Demo" notebook in R2R). `.env.local`:
```
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DEMO_NOTEBOOK_ID=<the-real-uuid-from-make-demo-setup>
```
Start `make r2r`, `make api`, `make web`. Wait for all three to come up.

| Step | Action | Expected |
|------|--------|----------|
| 3a.1 | Open `/demo`. Watch the "Try It Live" section. | A loading skeleton appears briefly (< ~2s), then is replaced by `ConversationView` (chat input + history pane). No cached-fallback badge appears. |
| 3a.2 | In the "Try It Live" input, type: `What was the company's revenue in 2023?` and submit. | Answer streams in. Citations `[1]`, `[2]`, ... appear inline. Citations reference documents from the Demo notebook only. |
| 3a.3 | In DevTools → Network → click the `POST /ask` (or `/conversations/.../messages/stream`) request → Payload. | Request body includes `"notebook_id": "<demo-uuid>"` matching `NEXT_PUBLIC_DEMO_NOTEBOOK_ID`. |

### 3b — Offline state (AC3.2)

Prerequisites: `.env.local` still has the demo notebook ID set, BUT stop `make api` (Ctrl-C the FastAPI process so port 8000 is dead).

| Step | Action | Expected |
|------|--------|----------|
| 3b.1 | Hard-refresh `/demo`. Watch the "Try It Live" section. | Loading skeleton appears for ≤ ~2s. |
| 3b.2 | After the skeleton, observe the panel. | A `Badge` with text exactly `Live demo unavailable — showing cached example` is rendered (secondary variant). Below the badge, an `AnswerCard` renders the answer + citation badges from `example_qa.json`. |
| 3b.3 | DevTools → Network → filter the `/health` request. | The `/health` request is present and shows status `(failed)` or a connection-refused error within ~2 seconds. |

### 3c — Unset notebook ID (AC3.3)

Prerequisites: Comment out or remove `NEXT_PUBLIC_DEMO_NOTEBOOK_ID` from `.env.local`. Restart `make web`. State of `make api` does not matter.

| Step | Action | Expected |
|------|--------|----------|
| 3c.1 | Open `/demo`. Watch the "Try It Live" section. | The section goes **directly** to the offline state (cached badge + `AnswerCard`). No 2-second skeleton wait — the effect short-circuits before fetching. |
| 3c.2 | DevTools → Network → look for any `/health` request. | **No** `/health` request is present. |
| 3c.3 | DevTools → Console. | No exceptions. No infinite loading skeleton. |

### 3d — Async after mount + 2s timeout (AC3.4)

Prerequisites: Set `NEXT_PUBLIC_DEMO_NOTEBOOK_ID=<some-uuid>` and point `NEXT_PUBLIC_API_URL=http://localhost:9999` (or any unbound port). Restart `make web`.

| Step | Action | Expected |
|------|--------|----------|
| 3d.1 | Open `/demo` with DevTools → Performance recording (or just observe with a stopwatch). | The three static panels (Wiki, Q&A, Figure) render **at first paint** — they are not blocked by the health probe. |
| 3d.2 | Watch the "Try It Live" skeleton transition. | Skeleton transitions to the cached fallback within ~2 seconds (the `AbortSignal.timeout(2000)` fires). |
| 3d.3 | DevTools → Network → find the `/health` request. | Status `(failed) net::ERR_CONNECTION_REFUSED` or a timeout-abort, with total duration ≤ ~2.1 seconds. |

---

## Phase 4: Demo Corpus Snapshots (AC4)

### 4a — End-to-end `make demo-setup` (AC4.1)

Prerequisites:
- `make r2r` running on 7272, `make api` running on 8000.
- `make demo-check` shows all green.
- Clean state: no "Demo" notebook in R2R. Stub `wiki_structure.json` / `example_qa.json` and 1×1 placeholder PNG committed.

| Step | Action | Expected |
|------|--------|----------|
| 4a.1 | Run `make demo-setup`. Watch stdout. | Six step headers print in order: notebook (create), download (Robinhood ARS PDF), ingest, wiki, Q&A, figure. Script exits 0. Final stdout includes a `DEMO_NOTEBOOK_ID=<uuid>` line. |
| 4a.2 | Inspect `apps/web/app/(app)/demo/data/wiki_structure.json`. | File size larger than the stub. `node -e "console.log(require('./apps/web/app/(app)/demo/data/wiki_structure.json').sections.length)"` reports ≥ 1. |
| 4a.3 | Inspect `apps/web/app/(app)/demo/data/example_qa.json`. | `answer` field is non-empty. `citations` array has length ≥ 1. |
| 4a.4 | Check `apps/web/public/demo/example_figure.png` size. | File size > 1024 bytes. Open the PNG and confirm it is a chart/table/page region. |

### 4b — Idempotency (AC4.2)

Immediately after step 4a succeeds, do not change anything.

| Step | Action | Expected |
|------|--------|----------|
| 4b.1 | Run `make demo-setup` a second time. | Output shows `Found existing 'Demo' notebook: <same-uuid>`. Output shows `All output files already contain real content — nothing to regenerate.` Exits without re-downloading, re-ingesting, or re-generating. |
| 4b.2 | Run `curl http://localhost:8000/notebooks`. | Exactly one notebook named "Demo" — not two. UUID matches the first run. |
| 4b.3 | Run `git diff -- apps/web/app/\(app\)/demo/data/wiki_structure.json apps/web/app/\(app\)/demo/data/example_qa.json apps/web/public/demo/example_figure.png`. | All three diffs are empty. |

### 4c — Real fintech content (AC4.3)

| Step | Action | Expected |
|------|--------|----------|
| 4c.1 | Open `wiki_structure.json`. Scan `sections[].title`. | Contains section titles relevant to Robinhood's 2023 filing (e.g., "Business Overview", "Risk Factors", "Financial Statements"). NOT `[{"title": "Overview"}]` alone. |
| 4c.2 | Open `example_qa.json`. Read `answer`. | Contains Robinhood-specific facts (revenue figures, transaction-based revenue, net interest revenue, etc.). `citations` has ≥ 1 entry referencing the Robinhood PDF. |
| 4c.3 | Open `apps/web/public/demo/example_figure.png` in an image viewer. | A real extracted figure — a chart, table, or page region from the filing. NOT a 1×1 white pixel. |
| 4c.4 | Run `make web`. Visit `/demo`. | All three static panels display Robinhood content. |

---

## End-to-End Scenarios

### E2E-1: Fresh-clone first-load smoke

**Purpose:** A new developer can demo the page with `make web` only.

1. Fresh checkout at `HEAD ae96925`. No `make demo-setup` ever run. Confirm stub data files and 1×1 placeholder PNG are committed.
2. Run `make setup`, then `make web`. Do **not** start `make api` or `make r2r`.
3. Navigate to `http://localhost:3000/demo`.
4. Confirm: sidebar shows "Demo"; all four section headings render; three static panels show stub content at first paint; "Try It Live" briefly shows a loading skeleton then transitions to the cached `AnswerCard` with the "Live demo unavailable" badge.
5. DevTools → Console: no errors, no unhandled promise rejections. Network: no 404s for `/demo/example_figure.png`.
6. Navigate to `/evals` — RAGAS table renders (Phase 1 did not accidentally delete the route).
7. Navigate back to `/demo` — state resets cleanly.

### E2E-2: Full-stack live demo

**Purpose:** End-to-end live wiring after Phase 4–5.

1. Phase 4 commits applied, `make demo-setup` not yet run.
2. Start `make r2r`, `make api`. Run `make demo-check` — confirm green.
3. Run `make demo-setup`. Wait 2–10 minutes.
4. Copy `DEMO_NOTEBOOK_ID=<uuid>` into `apps/web/.env.local` as `NEXT_PUBLIC_DEMO_NOTEBOOK_ID=<uuid>`. Ensure `NEXT_PUBLIC_API_URL=http://localhost:8000` is set.
5. Restart `make web`.
6. Open `/demo`. Confirm all three panels display real Robinhood content. "Try It Live" shows `ConversationView` (NOT cached fallback).
7. Ask: `What was the company's revenue in 2023?`. Confirm streamed answer with citations referencing the Robinhood PDF.
8. DevTools → Network → `POST /conversations/.../messages/stream` → Payload. Confirm `notebook_id` matches the demo UUID.
9. Run `make demo-setup` a second time. Confirm idempotency (`Found existing` + `All output files already contain real content`).
10. `git status` — only three intended snapshot paths modified vs. baseline.

### E2E-3: Toggle live ↔ cached via API uptime

**Purpose:** Validates DemoAskBox health-probe state machine in both directions.

1. Phase 5 complete. `apps/web/.env.local` has real demo notebook ID. `make web` running.
2. **API down, first load.** Stop `make api`. Hard-refresh `/demo`. Confirm "Try It Live" shows cached fallback badge within ~2 seconds.
3. **API up, second load.** Start `make api`. Wait for bind. Hard-refresh `/demo`. Confirm "Try It Live" shows `ConversationView`.
4. **API down, third load.** Stop `make api`. Hard-refresh `/demo`. Confirm "Try It Live" returns to cached fallback. No stale `ConversationView`, no infinite loading.
5. Across all three loads, confirm three static panels render identically regardless of API uptime.

---

## Human Verification Required (consolidated)

| Criterion | Why Manual | Steps |
|-----------|------------|-------|
| AC1.1 — "Demo" in sidebar, no "Evaluations" | DOM inspection of rendered `AppSidebar` | Phase 1, step 1.2 |
| AC1.2 — `/demo` renders without errors | Server Component cannot be rendered in jsdom | Phase 1, steps 1.3, 1.5 |
| AC1.3 — `/evals` still routable | Server Component route existence | Phase 1, step 1.4 |
| AC1.4 — No 404 / runtime error first load | Requires real browser + Network panel | Phase 1, step 1.5; E2E-1 step 5 |
| AC2.1 — WikiPreviewPanel renders without API call | Server Component + "no network egress" assertion | Phase 2, step 2.1; E2E-1 step 4 |
| AC2.2 — QAShowcasePanel renders answer + citation | Server Component composing AnswerCard | Phase 2, step 2.2 |
| AC2.3 — Figure panel renders image + caption | Static `<img>` rendering + 200 status check | Phase 2, step 2.3 |
| AC2.4 build leg | TypeScript compile-time check | Phase 2, steps 2.4a, 2.4b |
| AC2.4 operational leg (wiki shape) | Requires real Phase-5 snapshot | Phase 2, step 2.4c; E2E-2 step 6 |
| AC3.1 — Live branch | `fetch`-on-mount client component | Phase 3a; E2E-2 steps 6–8; E2E-3 step 3 |
| AC3.2 — Cached fallback on health failure | Visual badge assertion | Phase 3b; E2E-1 step 4; E2E-3 steps 2, 4 |
| AC3.3 — Unset env var short-circuits | `NEXT_PUBLIC_*` is build-time baked | Phase 3c |
| AC3.4 — 2s timeout, non-blocking | Timing-sensitive observation | Phase 3d; E2E-1 step 4 |
| AC4.1 — `make demo-setup` end-to-end | Imperative Shell script; FCIS convention verifies operationally | Phase 4a; E2E-2 step 3 |
| AC4.2 — Idempotency | Cross-process state (R2R + filesystem) — requires real run | Phase 4b; E2E-2 step 9 |
| AC4.3 — Real fintech content | Content-quality judgement — requires human eye | Phase 4c; E2E-2 step 6 |

---

## Traceability

| Acceptance Criterion | Automated Test | Build Gate | Manual Step |
|----------------------|----------------|------------|-------------|
| AC1.1 | — | — | Phase 1 step 1.2; E2E-1 step 4 |
| AC1.2 | — | — | Phase 1 steps 1.3, 1.5; E2E-1 step 4 |
| AC1.3 | — | — | Phase 1 step 1.4; E2E-1 step 6 |
| AC1.4 | — | — | Phase 1 step 1.5; E2E-1 step 5 |
| AC2.1 | — | — | Phase 2 step 2.1; E2E-1 step 4; E2E-2 step 6 |
| AC2.2 | — | — | Phase 2 step 2.2; E2E-1 step 4; E2E-2 step 6 |
| AC2.3 | — | — | Phase 2 step 2.3; E2E-1 step 4; E2E-2 step 6 |
| AC2.4 (qa shape) | — | `npx tsc --noEmit` on `QAShowcasePanel.tsx:5` | Phase 2 steps 2.4a, 2.4b |
| AC2.4 (wiki shape) | — | — | Phase 2 step 2.4c; E2E-2 step 6 |
| AC3.1 | — | — | Phase 3a; E2E-2 steps 6–8; E2E-3 step 3 |
| AC3.2 | — | — | Phase 3b; E2E-1 step 4; E2E-3 steps 2, 4 |
| AC3.3 | — | — | Phase 3c |
| AC3.4 | — | — | Phase 3d; E2E-1 step 4 |
| AC4.1 | — | — | Phase 4a; E2E-2 step 3 |
| AC4.2 | — | — | Phase 4b; E2E-2 step 9 |
| AC4.3 | — | — | Phase 4c; E2E-2 step 6 |

---

## Regression Gate

```
make test
```

Must remain green on the branch. This plan is additive (new files in `apps/web/app/(app)/demo/`, one nav line edit in `AppSidebar.tsx`, one new `scripts/setup_demo_corpus.py`, one new Makefile target); no existing test should regress.
